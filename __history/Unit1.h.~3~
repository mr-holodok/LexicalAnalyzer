//---------------------------------------------------------------------------

#ifndef Unit1H
#define Unit1H

#include "DataItem.h"
//#include <vector>

class HashTable
{
private:
	const static int size = 100;
	DataItem hashArray[size];

public:
	__fastcall HashTable();
	void insert(std::string); //this function gets index from hashFunction
	bool find(std::string);

	//==== it is public only for a test!!=======
	int hashFunction(double key);  //this function gets key from DataItem
	double generateKey(std::string);
};

//---------------------------------------------------------------------------
#endif
